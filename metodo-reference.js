const ms=[...document.querySelectorAll('.metodo-step')],mn=document.querySelector('.metodo-num'),mt=document.querySelector('.metodo-title');function syncMetodo(){if(!ms.length)return;let best=ms[0],dist=1e9,c=innerHeight/2;ms.forEach(x=>{const r=x.getBoundingClientRect(),d=Math.abs(r.top+r.height/2-c);if(d<dist){dist=d;best=x}});ms.forEach(x=>x.classList.toggle('active',x===best));if(mn)mn.textContent=best.dataset.num||'01';if(mt)mt.textContent=best.dataset.title||''}addEventListener('scroll',syncMetodo,{passive:true});syncMetodo();
